% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_synthetic_samples.R
\name{create_synthetic_samples}
\alias{create_synthetic_samples}
\title{Generate synthetic clustered samples with isotropic Gaussian noise}
\usage{
create_synthetic_samples(
  n_samples,
  n_clusters,
  std_dev,
  n_dim,
  cube_size = 100,
  standardize = FALSE,
  center = TRUE,
  scale. = TRUE
)
}
\arguments{
\item{n_samples}{Total number of samples to generate.}

\item{n_clusters}{Number of clusters to simulate.}

\item{std_dev}{Standard deviation of the Gaussian noise around each centroid.}

\item{n_dim}{Number of dimensions (features).}

\item{cube_size}{Side length of the hypercube where centroids are placed.}

\item{standardize}{Logical; if \code{TRUE}, standardise the final dataset
(mean 0, sd 1 per feature).}

\item{center, scale.}{Logical arguments passed to \code{scale()} if
\code{standardize = TRUE}.}
}
\value{
A data frame of size \code{n_samples × n_dim} containing the
synthetic samples.
}
\description{
This function generates synthetic datasets composed of \code{n_clusters}
Gaussian clusters in \code{n_dim}-dimensional space. Cluster centroids are
placed uniformly inside a hypercube of side \code{cube_size}, and samples
are drawn with isotropic Gaussian noise of standard deviation \code{std_dev}.
}
\details{
The function is used internally to calibrate the compactness of real data
by matching its AMD peak against synthetic datasets with varying noise levels.
}
\examples{
\dontrun{
set.seed(1)
syn <- create_synthetic_samples(
  n_samples = 200,
  n_clusters = 4,
  std_dev = 5,
  n_dim = 10
)
head(syn)
}

}
