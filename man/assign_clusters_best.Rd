% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_clusters_best.R
\name{assign_clusters_best}
\alias{assign_clusters_best}
\title{Select the best fuzzy c-means partition across repeated initialisations}
\usage{
assign_clusters_best(
  data,
  opt_cluster,
  nreps = 10,
  m = 2,
  iter.max = 20,
  scale_data = FALSE,
  seeds = NULL,
  preselect_top_sd = NULL
)
}
\arguments{
\item{data}{A numeric matrix or data frame of samples × features.}

\item{opt_cluster}{Integer; number of clusters to fit.}

\item{nreps}{Number of repeated initialisations.}

\item{m}{Fuzziness parameter for fuzzy c-means (default 2).}

\item{iter.max}{Maximum number of iterations for fuzzy c-means.}

\item{scale_data}{Logical; if \code{TRUE}, standardise features before clustering.}

\item{seeds}{Optional numeric vector of seeds for deterministic behaviour.
Must have length \code{nreps}. If \code{NULL}, random seeds are drawn.}

\item{preselect_top_sd}{Optional integer; if provided, only the top-SD features
are retained before clustering (useful for very high-dimensional data).}
}
\value{
A list with components:
\describe{
\item{cluster}{Integer vector of cluster labels aligned to the original data.
Rows with missing values receive \code{NA}.}
\item{membership}{Membership matrix from the best fuzzy c-means run.}
\item{centers}{Cluster centroids from the best run.}
\item{Mpm}{Best AMD-like objective value.}
}
}
\description{
This function runs fuzzy c-means clustering (\code{e1071::cmeans}) repeatedly
with different random seeds and selects the partition that maximises an
AMD-like objective:
}
\details{
\deqn{ \mathrm{Mpm} = \mathrm{mean}(\max_i u_i) - 1/k }

where \eqn{u_i} is the membership vector of sample \eqn{i}.
The best partition is returned, with cluster labels aligned to the original
row order of the input data (rows with missing values receive \code{NA}).
}
\examples{
\dontrun{
set.seed(1)
X <- matrix(rnorm(1000), nrow = 100, ncol = 10)
out <- assign_clusters_best(X, opt_cluster = 3, nreps = 20)
table(out$cluster)
}

}
